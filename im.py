from PIL import Image
import numpy as np
import matplotlib.pyplot as plt
#Generating a Configuration with random spins
config = np.random.choice([-1, 1], size=(10, 10))
print(config)
config np.random. choice([-1, 1], size=(200, 200))
geromarray(np.uint8 ( (config + 1) * 0.5 * 255
